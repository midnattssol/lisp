(noop (let _insert_into_sorted (closure (expression (vector item _list) (do (let l (len _list)) (let i (sub l 1)) (let ptr l) (while (expression (geq i 0)) (expression (do (let ptr (ternary (gt (get i _list) item) i ptr)) (let i (sub i 1))))) (linsert item ptr _list))))) (let insertion_sort (closure (expression (vector _list) (do (let out (list)) (let i 0) (let l (len _list)) (while (expression (lt i l)) (expression (do (let out (_insert_into_sorted (get i _list) out)) (let i (add i 1))))) out)))) (insertion_sort (rand 100)))
